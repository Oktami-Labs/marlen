node_modules/
dist/
*.local
.env
.env.*
!.env.example
# Runtime state: SQLite DB + stored credentials (auth.json)
apps/server/data/
# Agent home (memory/, skills/, knowledge/); the desktop app keeps it under userData
agent-home/
# Assembled desktop app (bundles + staged node_modules); installers land in
# apps/desktop/dist/, covered by the global dist/ rule above.
apps/desktop/build/
# Playwright run artifacts (traces, screenshots, videos, HTML report)
apps/e2e/test-results/
apps/e2e/playwright-report/
apps/e2e/blob-report/
.claude/settings.local.json
.DS_Store
*.log
*.db
*.db-wal
*.db-shm
*.db-journal
CONTEXT.md

# Private per-customer install bundles with an agent home and setup skill.
setup-template/

# Private tool that seeds fixture mail into a test Gmail account.
demo-seed/
*.tsbuildinfo

# Rust output from the parallel Tauri desktop shell.
apps/desktop-tauri/src-tauri/target/
apps/desktop-tauri/src-tauri/gen/
