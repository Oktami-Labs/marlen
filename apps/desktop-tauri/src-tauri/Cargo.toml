[package]
name = "marlen-desktop-tauri"
version = "0.5.1"
description = "Marlene desktop shell preview"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
